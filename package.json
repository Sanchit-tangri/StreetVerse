{
  "name": "streetverse",
  "version": "1.0.0",
  "private": true,
  "description": "StreetVerse - Full-stack application with Next/Vite React frontend and Express WebSocket PostgreSQL backend",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build",
    "server:install": "npm --prefix server install",
    "client:install": "npm --prefix client install",
    "install:all": "npm run server:install && npm run client:install",
    "server:dev": "npm --prefix server run dev",
    "client:dev": "npm --prefix client run dev",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "tunnel": "powershell -ExecutionPolicy Bypass -File .\\start-tunnel.ps1 -Port 5000"
  },
  "keywords": ["streetverse", "fullstack", "react", "express", "postgres", "websockets"],
  "author": "Sanchit Tangri",
  "license": "MIT"
}
